# target-cpu=native: use this machine's SIMD set (AVX2 and so on).
# The price: the produced binary may not run on a different/older CPU — since
# this is a learning project, local performance was chosen over portability
# (DECISIONS.md, the SIMD section).
[build]
rustflags = ["-C", "target-cpu=native"]
